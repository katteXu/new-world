import React from 'react';
import { Button } from 'antd';

interface AppProps {}

function App({}: AppProps) {
  return (
    <div className="App">
      <Button>按钮2422</Button>
    </div>
  );
}

export default App;
